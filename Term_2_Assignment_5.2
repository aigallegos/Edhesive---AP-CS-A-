import java.util.*;

public class Main {

  public static void main (String[] args) {
    GameWheel gameWheel = new GameWheel();
    ArrayList<PrizeCard> player1 = new ArrayList<PrizeCard>();
    ArrayList<PrizeCard> player2 = new ArrayList<PrizeCard>();
    int p1 = 0;
    while (p1 < 5){
      PrizeCard spin = gameWheel.spinWheel();
      if (!player1.contains(spin)){
        player1.add(spin);
        p1++;
      }
    }
    int p2 = 0;
    while (p2 < 5){
      PrizeCard spin = gameWheel.spinWheel();
      if (!player2.contains(spin)){
        player2.add(spin);
        p2++;
      }
    }

    int prizeTotal1 = 0;
    int prizeTotal2 = 0;

    for (int i = 0; i<5; i++) {
      prizeTotal1 += player1.get(i).getPrizeAmount();
      prizeTotal2 += player2.get(i).getPrizeAmount();
    }

    System.out.println("Player 1 Total: $" + prizeTotal1);
    for (PrizeCard card: player1)
      System.out.println(card);
    System.out.println("\nPlayer 2 Total: $" + prizeTotal2);
    for (PrizeCard card: player2)
      System.out.println(card);

    System.out.println();

    if (prizeTotal1 > prizeTotal2) {
      System.out.println("Player 1 won by $" + (prizeTotal1 - prizeTotal2) + "!");
    }
    else {
      System.out.println("Player 2 won by $" + (prizeTotal2 - prizeTotal1) + "!");
    }
  }

}
